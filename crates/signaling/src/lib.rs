//! WebRTC / SFU call signaling — placeholder for roadmap step 8 (calls).
//!
//! Intentionally empty for now. It exists so the workspace layout and the boundary for calls
//! are established up front; signaling will ride the same versioned WebSocket transport and the
//! `calls` capability when 1:1 then group calls land.
